#include <Arduino.h>
// #include "oled.h"
#include "rgb.h"


// OLED oled;
RGB rgb;

void setup() {
  
  // oled_init();
  //oled_print("siema", 0, 10);
  // oled_drawBox();
   

  // pinMode(IN1, OUTPUT);
  // pinMode(IN2, OUTPUT);

  // pinMode(IN3, OUTPUT);
  // pinMode(IN4, OUTPUT);
}

void loop() {

  
}

